kind: Service
apiVersion: v1
metadata:
  name: mysql
  labels:
    name: mysql
spec:
  ports:
    - name: server
      protocol: TCP
      port: 3306
      targetPort: 3306
  selector:
    name: mysql
  type: LoadBalancer
